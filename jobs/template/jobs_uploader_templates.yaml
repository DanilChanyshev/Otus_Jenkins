- job-template:
    name: 'jobs_uploader'
    description: "<h3 style='color: red;'>jobs_uploader</h3>"
    project-type: pipeline
    concurrent: false
    parameters:
      - string:
          name: REFSPEC
          description: "Branch name"
          default: master
    pipeline-scm:
      scm:
        - uploader_scm
      script-path: ./pipeline/jobs_uploader.groovy